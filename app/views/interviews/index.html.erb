<h1>面接一覧</h2>
<ul class="interviews">
  <% if @interviews %>
    <% @interviews.each do |interview| %>
      <li>
        <%= interview.scheduled_at.strftime("%Y年%-m月%-d日 %-H時%-M分") %>
        <%= interview.status %>
        
      </li>
    <%end%>
  <%end%>
</ul>